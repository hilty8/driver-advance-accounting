# 認証/遷移ルール

## ロール別遷移ルール

- admin: `/admin`
- operator: `/admin`
- company: `/company`
- driver: `/driver`

## 未ログイン時のガード

- 保護ページへ未ログインでアクセスした場合、`/login` へリダイレクトする。

## 権限不足時の扱い

- 403 を受けた場合はアクセス拒否の表示を行う。
- 画面内の「ホームへ戻る」からロール別ホームへ遷移できる。
