{
  "name": "driver-advance-accounting-backend",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "ts-node src/main.ts",
    "dev:reset": "npx prisma migrate dev && SEED_MODE=dev npm run db:seed",
    "build": "tsc -p tsconfig.json",
    "test:int": "jest -c jest.int.config.ts --runInBand",
    "test:scenario": "jest -c jest.scenario.config.ts --runInBand",
    "test:unit": "jest -c jest.unit.config.ts",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:migrate:reset": "prisma migrate reset",
    "db:seed": "prisma db seed",
    "db:reset": "ts-node scripts/dbReset.ts",
    "test": "jest",
    "lint": "echo 'lint not configured yet'"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "@types/nodemailer": "^7.0.4",
    "japanese-holidays": "^1.0.10",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^7.0.12",
    "stripe": "^20.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^20.11.17",
    "@types/supertest": "^6.0.3",
    "dotenv": "^17.2.3",
    "jest": "^29.7.0",
    "prisma": "^5.10.0",
    "supertest": "^7.1.4",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  }
}
